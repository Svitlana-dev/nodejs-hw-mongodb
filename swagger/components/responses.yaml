Ok:
  description: Successful response
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yaml#/ResponseOk'

Created:
  description: Successfully created
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yaml#/ResponseOk'

Updated:
  description: Successfully updated
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yaml#/ResponseOk'

NoContent:
  description: Successfully deleted (no content)
  content: {}

BadRequest:
  description: Bad request
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yaml#/ResponseError'

Unauthorized:
  description: Unauthorized
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yaml#/ResponseError'

NotFound:
  description: Not found
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yaml#/ResponseError'
